function checkSet(){
    // Only change code below this line
    var set = new Set();
    set.add(1)
    set.add(2)
    set.add(3)
    set.add(4)
    set.add(5)
    set.delete(2)
    set.delete(5)

    // Only change code above this line
    return set;   
  }

checkSet()  